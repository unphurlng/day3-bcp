var countTo = prompt('how high should we count even numbers?');

// (init; condition; post-loop)
if(sure) {
    for(var i = 0; i < countTo; i++) {
        var y = i % 2;
        if(y === 0) {
            console.log('the number', i, 'is under', countTo);
        }
}
